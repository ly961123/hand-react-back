import React, {
  memo,
} from 'react';
import { Button } from 'antd';

const Shop = memo(() => {
  return (
    <div>
      <Button
        type='primary'
      >详情哦</Button>
    </div>
  )
})

export default Shop;